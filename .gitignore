# Device backups: private user audio, large, never committed.
*.pak
*.ppak

# Sanitized SysEx capture fixtures are intentionally NOT ignored;
# scrub serial numbers and sample audio before committing one.

# Pinned upstream checkout used for protocol cross-checks.
.upstream/

# Transient device state dumps from tools/assign_test.py
tools/assign_test_state.json

# Parallel-agent worktrees live here; never part of this repo.
.claude/worktrees/
.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# Local release artifacts.
dist/
